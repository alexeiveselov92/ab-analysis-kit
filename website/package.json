{
  "name": "ab-analysis-kit-website",
  "private": true,
  "type": "module",
  "description": "Marketing landing + documentation site for abkit (abkit.pipelab.dev).",
  "scripts": {
    "sync": "node scripts/sync-docs.mjs",
    "dev": "npm run sync && astro dev",
    "start": "npm run dev",
    "build": "npm run sync && astro build",
    "preview": "astro preview",
    "check": "astro check",
    "check:demo-parity": "node scripts/check-demo-parity.mjs"
  },
  "dependencies": {
    "@astrojs/starlight": "^0.40.0",
    "astro": "^6.4.8",
    "sharp": "^0.35.2"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.4",
    "typescript": "^5.9.0"
  }
}
