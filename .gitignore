# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual environments
venv/
ENV/
env/
.venv

# Testing
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
# Windows/WSL download artifacts (NTFS alternate-data-stream markers)
*:Zone.Identifier

# Jupyter
.ipynb_checkpoints
*.ipynb

# Mypy / Ruff
.mypy_cache/
.dmypy.json
dmypy.json
.ruff_cache/

# Project specific (an abk project scaffolded for local use)
abkit_project.yml
profiles.yml
*.log
.env
*.zip

# abk runtime outputs (self-contained reports + tune write-back history)
reports/
**/.history/

# Website (Astro)
node_modules/
website/dist/

# Design-tool exports (DesignCode/dc) — generated artifacts, never committed
*.dc.html
*.dc *.html

# Claude Code: project config is SHARED so contributing works out of the box.
# CLAUDE.md and everything under .claude/ (rules, skills, shared settings) is
# committed. Only personal, per-machine overrides are ignored.
.claude/*.local.json
