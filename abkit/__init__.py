"""ab-analysis-kit — A/B experiment analysis as declarative YAML + SQL.

Pre-development: the package layout, statistical engine, pipeline and CLI are
specified in ``docs/specs/`` and built per ``ROADMAP.md``. This module currently
exposes only ``__version__`` (the single source of truth for the CLI and the
``abk init-claude`` version stamp).
"""

__version__ = "0.0.1.dev0"
